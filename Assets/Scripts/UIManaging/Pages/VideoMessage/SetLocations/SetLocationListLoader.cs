using System.Collections.Generic;
using System.Threading.Tasks;
using Bridge;
using Bridge.Models.ClientServer.Assets;

namespace UIManaging.Pages.VideoMessage.SetLocations
{
    /*internal sealed class SetLocationListLoader
    {
        private readonly IBridge _bridge;
        private const int PAGE_SIZE = 20;
        private long? _lastLoadedSetLocationId;
        private Dictionary<long, List<SetLocationFullInfo>>

        public SetLocationListLoader(IBridge bridge)
        {
            _bridge = bridge;
        }

        public async Task<SetLocationFullInfo[]> LoadNextPage()
        {
            await _bridge.GetSetLocationListAsync(_lastLoadedSetLocationId, )
        }
    }*/
}